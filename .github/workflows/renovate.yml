name: Renovate Application
on:
  # Daily at 02:00 Europe/Berlin
  schedule:
    - cron: '0 2 * * *'
      timezone: "Europe/Berlin"
  # Manual trigger
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Renovate log level'
        required: false
        default: 'info'
        type: choice
        options:
          - info
          - debug
          - warn
          - error

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  renovate:
    runs-on: self-hosted
    steps:
      # GitHub App installation token (guided-traffic-automation), scoped to this
      # repository with all permissions of the app; expires after 1 h and is
      # revoked at the end of the job. Renovate detects the app identity itself.
      - name: Create GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v7

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@v46.3.4
        with:
          token: ${{ steps.app-token.outputs.token }}
          configurationFile: renovate.json
        env:
          LOG_LEVEL: ${{ inputs.logLevel || 'info' }}
          RENOVATE_AUTODISCOVER: 'true'
          RENOVATE_AUTODISCOVER_FILTER: 'guided-traffic/s3-encryption-proxy'
          RENOVATE_REQUIRE_CONFIG: 'required'
