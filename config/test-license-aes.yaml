# Test configuration for license system with encryption (should fail without license)
bind_address: "0.0.0.0:8090"
log_level: "info"

# S3 backend (fake for testing)
target_endpoint: "http://localhost:9999"
region: "us-east-1"

# AES provider configuration - should require license
encryption:
  encryption_method_alias: "test-aes"
  providers:
    - alias: "test-aes"
      type: "aes"
      description: "AES encryption provider"
      config:
        aes_key: "XZmcGLpObUuGV8CFOmfLKs7rggrX2TwIk5/Lbt9Azl4="
